{
  "name": "@dustinrouillard/fastify-utilities",
  "version": "1.0.0",
  "author": "Dustin Rouillard <contact@dustin.sh>",
  "description": "Utilities for dustin.sh fastify boilerplate",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "push": "yarn build; cp -R package.json README.md LICENSE dist; (cd dist && yarn publish --non-interactive); rm -rf dist/package.json dist/README.md dist/LICENSE",
    "lnk": "yarn build; cp -R package.json README.md LICENSE dist; (cd dist && yarn link)"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "dependencies": {
    "chalk": "^4.0.0",
    "fastify": "^2.14.1",
    "fastify-plugin": "^1.6.1"
  },
  "devDependencies": {
    "@types/node": "^14.0.6"
  }
}